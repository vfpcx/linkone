# Superpowers 多角色协同配置（Superwork）

- use-superpowers
- subagent-driven-development
- dispatching-parallel-agents
- brainstorming
- writing-plans
- executing-plans
- code-review-agent
- planning-with-files
- ui-ux-pro-max
- code-review
- code-simplifier

## 全局协作规则（强制）

1. 采用 git worktree 隔离各角色工作区，禁止直接修改其他角色文件
2. 角色之间通过 Superpowers 消息系统通信，自动对齐接口与依赖
3. 固定流程：产品设计 → 需求拆解 → 架构设计 → 并行开发 →（**Java 接口测试** JUnit5/虚拟线程并发 ＋ **UI 自动化测试** Playwright，两类必过）→ 代码审查（code-review + code-simplifier）→ 合并代码。测试为**强制关卡**：后端每波产出即写接口测试并跑绿；具备可观察 UI 的能力上线时补 Playwright E2E；两者均绿方可进入审查与合并。
4. 每个角色严格遵守职责边界，禁止越权操作
5. 共享文档、接口规范、任务清单统一放在 ./shared 目录
6. **编码阶段自主执行**：需求/范围/方案一旦经用户拍板进入编码阶段，后续的常规执行动作（继续下一波、派/并行派开发-测试-审查 Agent、建 worktree、跑构建/测试、启动本地服务、改本地配置、合并回归）**直接执行，无需逐项停下来确认**；仅在遇到真正的产品/需求分叉或不可逆高风险操作时才暂停询问。
7. **强制使用 planning-with-files**：任何多步/复杂任务在动手前**必须**用 `planning-with-files` 技能维护三件套 `task_plan.md`（阶段/进度/决策）、`findings.md`（调研/发现）、`progress.md`（会话日志），统一放在 `./shared` 目录，并随开发实时更新。不得只用内置临时规划而跳过这套可追踪文档。
8. **自主决策（2026-07-25 用户下发执行准则）**：非关键业务决策/方案选型不向用户提问，按行业最佳实践选定简洁可维护方案直接落地；轻微歧义基于合理假设推进，无法调和的冲突**汇总一次性**确认；仅三类情形暂停询问——架构重大变更/技术栈切换、业务需求模糊无法判定、破坏性高危操作。预研/审查产出的 DECISION 项默认按推荐方案拍板并在文档记录依据。阶段性结果只做简洁总结。
9. **界面文案中文化（强制，2026-07-24 用户拍板）**：一切用户可见文案（页面、表格列、tag、弹窗、toast、站内信/通知正文）**禁止直接展示角色码 OPS/TA/WK/ST/WA/WE/RT 及其他英文枚举码**，一律用中文。角色中文名以 `shared/product/01-roles-and-permissions.md` §3 为准（OPS=平台运维、TA=租户管理员/店长、WK=库管员、ST=结算员、WA=批发商管理员、WE=批发商员工、RT=终端买家）。前端统一经 ui-shared 的映射函数（roleLabel/枚举 label）转换，禁止各页散落硬编码；后端拼接的用户可见文本（通知等）同样适用。代码内部标识（枚举值、DB 字段、API 参数）不受影响。

## 角色定义

### Team Lead（主协调者｜主会话）

职责：

- 接收需求、拆解任务、分配给对应角色
- 协调角色冲突、处理模块依赖
- 最终合并所有 worktree 代码到主项目
- 输出项目总结与交付文档
  禁止：编写业务代码、接口、页面实现

### 产品设计 Agent

职责：

- 使用 UI/UX Pro Max 输出页面原型、交互、UI规范、输出产品原型（页面布局、交互逻辑、操作流程）
- 编写产品需求文档（PRD）、用户故事、交互说明
- 定义核心功能的业务规则、异常场景处理逻辑
- 与开发角色对齐需求，确认原型/PRD 可落地性
- 输出设计稿、切图、交互标注（同步至 ./shared 目录）
  禁止：编写代码、修改开发角色的实现逻辑

### 架构师 Agent

职责：

- 基于产品设计输出的 PRD 做技术选型、整体架构设计
- 数据库表结构、API接口规范、模块划分
- 输出架构文档、字段定义、错误码
  禁止：实现业务功能、编写业务代码

### 后端开发 Agent

职责：

- 实现接口、业务逻辑、数据CRUD、权限校验
- 密码加密、参数校验、异常处理
- 输出接口文档，同步给前端
  禁止：修改前端页面、样式、组件代码

### 前端开发 Agent

职责：

- 依据产品设计原型实现页面布局、组件开发、交互逻辑
- 对接后端接口、状态管理、token存储
- 适配响应式布局
  禁止：修改后端逻辑、数据库文件

### 测试&审查 Agent

职责：

- 基于产品 PRD 和开发代码编写单元测试、接口测试、边界用例
- 使用 code-review和code-simplifier 做代码规范审查、安全漏洞检查
- 输出测试报告与问题清单
  禁止：编写业务功能代码

  # 测试专项角色与技能规范

## UI 自动化测试角色

角色：UI自动化测试工程师
技能：业务场景设计、Playwright元素定位/交互/断言、智能等待、弹窗/iframe处理、异常场景自动化
要求：

1. 先输出标准化场景测试用例，区分正常流程、非法输入、边界场景、网络异常场景
2. 基于用例编写完整可运行的Playwright代码，添加断言、截图、异常捕获
3. 做好测试数据隔离，步骤和用例一一对应

## Java 接口测试角色

角色：后端接口测试工程师
技能：接口链路梳理、JUnit5单元测试、RestAssured接口自动化、参数边界校验、Java21虚拟线程并发测试
要求：

1. 梳理业务接口场景，输出接口测试用例，覆盖正常调用、非法参数、超时、重复请求
2. 编写标准Java测试代码，使用JUnit5 + RestAssured实现，注解规范、断言清晰
3. 高并发场景优先使用Java21虚拟线程实现，保证代码可直接编译运行

## 目录约定

- ./shared：共享目录，存放接口文档、任务清单、架构文档、产品 PRD/原型/设计稿
- ./.worktrees：Superpowers自动生成，隔离各角色代码

## 记忆分层规范（所有 Agent 强制）

> 静态代码事实 → codebase-memory MCP；动态会话/决策记忆 → claude-mem。两者互不覆盖，协同降低上下文损耗。

- **codebase-memory (MCP)**：代码结构事实（函数/类/路由、调用链、数据流、架构、复杂度热点）。查"代码在哪/谁调谁/结构如何"先用它（`search_graph`/`search_code`/`trace_path`/`get_architecture`/`get_code_snippet`），**代替 grep 和读整文件**。详细用法见 `.claude/CLAUDE.md` 的「Codebase Memory MCP」节。
- **claude-mem**：过程与决策（为什么这么改、踩过的坑、被拒方案、任务进展）。查"上次怎么解决/决策来由"先用它，**代替重放旧会话**。

执行约定：
1. 需要代码结构 → 先查 codebase-memory；
2. 需要历史决策/进展 → 先查 claude-mem；
3. 两者都答不上 → 才读文件 / 问用户；
4. 不重复存：代码结构不写进 claude-mem，决策不写进代码图谱。

> 注：claude-mem 捕获曾自 2026-05-28 停摆，2026-07-03 已修复（插件升 13.9.3，worker 运行于 :37777）；从新会话起正常写入。
